# tb/configs/ Directory

This directory holds project-specific configuration files:
- *.svi files (SystemVerilog interface definitions)
- UVM configuration definitions
- VIP configuration overrides
- Compile-time defines

NOTE: The svt_*.svi files are provided by Synopsys SVT VIP installation
($SVT_HOME). Those are NOT part of this project. Place PROJECT-SPECIFIC
config *.svi files here ONLY.

Currently holding: (project-specific configs go here)